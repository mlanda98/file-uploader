<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>File Details</title>
</head>
<body>
  <h1>File Details</h1>
  <p><strong>Name:</strong> <%= file.name %></p>
  <p><strong>Size:</strong> <%= (file.size / 1024).toFixed(2) %></p>
  <p><strong>Uploaded At:</strong> <%= new Date(file.uploadTime).toLocaleString() %></p>

  <h2>Actions</h2>
  <a href="<%= file.url %>" download="<%= file.name %>">
    <button>Download</button>
  </a>
</body>
</html>